import React from "react";
import { AreaCard } from "src/components/AreaCard";

export const DummyComponent = () => {
	return (
		<div>
			<AreaCard name="Tokyo" onClick={() => {}} />
		</div>
	);
};
